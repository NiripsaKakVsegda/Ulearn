export const enum GroupSettingsTab {
	Settings = 'settings',
	Members = 'members',
	AdditionalContent = 'additional-content',
	DeadLines = 'dead-lines'
}

export const enum GroupsListTab {
	Active = 'active',
	Archive = 'archive',
}
